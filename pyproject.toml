[project]
name = "opt-bench"
version = "0.1.0"
description = "An LLVM IR dataset for data-driven compiler optimization research"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
    "tqdm>=4.67.3",
    "unidiff>=0.7.5",
]
