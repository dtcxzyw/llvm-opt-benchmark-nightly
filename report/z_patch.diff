diff --git a/llvm/lib/Transforms/InstCombine/InstCombineAndOrXor.cpp b/llvm/lib/Transforms/InstCombine/InstCombineAndOrXor.cpp
index 5f9fecc7c5675..e1b7511d50ea4 100644
--- a/llvm/lib/Transforms/InstCombine/InstCombineAndOrXor.cpp
+++ b/llvm/lib/Transforms/InstCombine/InstCombineAndOrXor.cpp
@@ -5435,6 +5435,20 @@ Instruction *InstCombinerImpl::visitXor(BinaryOperator &I) {
           MaskedValueIsZero(X, *C, &I))
         return BinaryOperator::CreateXor(X, ConstantInt::get(Ty, *C ^ *RHSC));
 
+      // xor (zext (or X, C)), RHSC --> xor (zext X), (RHSC ^ zext(C))
+      // iff X & C == 0.
+      auto FoldZExtOr = [&]() -> Instruction * {
+        Value *ZExt = Builder.CreateZExt(X, Ty);
+        APInt ZExtC = C->zext(RHSC->getBitWidth());
+        return BinaryOperator::CreateXor(ZExt,
+                                         ConstantInt::get(Ty, *RHSC ^ ZExtC));
+      };
+      if (match(Op0, m_OneUse(m_ZExt(m_c_DisjointOr(m_Value(X), m_APInt(C))))))
+        return FoldZExtOr();
+      if (match(Op0, m_OneUse(m_ZExt(m_c_Or(m_Value(X), m_APInt(C))))) &&
+          MaskedValueIsZero(X, *C, &I))
+        return FoldZExtOr();
+
       // When X is a power-of-two or zero and zero input is poison:
       // ctlz(i32 X) ^ 31 --> cttz(X)
       // cttz(i32 X) ^ 31 --> ctlz(X)
