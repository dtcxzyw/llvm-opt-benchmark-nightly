diff --git a/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp b/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
index 4066f41463d15..a6c5d61be2234 100644
--- a/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
+++ b/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
@@ -559,9 +559,16 @@ static bool isKnownNoWrap(Value *V, const ConstraintInfo &Info, bool Signed) {
   Value *Op1 = BO->getOperand(1);
   auto Opcode = static_cast<Instruction::BinaryOps>(BO->getOpcode());
 
-  // Op0 - Op1 does not wrap unsigned if Op0 >=u Op1.
-  if (Opcode == Instruction::Sub)
-    return !Signed && Info.doesHold(CmpInst::ICMP_UGE, Op0, Op1);
+  if (Opcode == Instruction::Sub) {
+    // Op0 - Op1 does not wrap unsigned if Op0 >=u Op1.
+    if (!Signed)
+      return Info.doesHold(CmpInst::ICMP_UGE, Op0, Op1);
+
+    // Op0 - Op1 does not wrap signed if 0 <=s Op1 <=s Op0.
+    if (Info.isKnownNonNegative(Op1) &&
+        Info.doesHold(CmpInst::ICMP_SGE, Op0, Op1))
+      return true;
+  }
 
   if (!Signed && BO->hasNoSignedWrap() &&
       (Opcode == Instruction::Shl || Info.isKnownNonNegative(Op1)) &&
@@ -1389,9 +1396,11 @@ static bool canStrengthenFlags(Instruction *I) {
 
   switch (BO->getOpcode()) {
   case Instruction::Sub:
-    // A - B does not wrap unsigned, if A >=u B. Subs with constant operands get
-    // canonicalized to Add.
-    return !BO->hasNoUnsignedWrap() && !isa<Constant>(BO->getOperand(1));
+    if (BO->hasNoUnsignedWrap() && BO->hasNoSignedWrap())
+      return false;
+    // A - B does not wrap unsigned if A >=u B, and does not wrap signed if
+    // 0 <=s B <=s A. With a constant B, bounds on A can refine both flags.
+    return true;
   case Instruction::Add:
   case Instruction::Mul:
   case Instruction::Shl:
diff --git a/llvm/test/Transforms/ConstraintElimination/decompose-signed-preconditions.ll b/llvm/test/Transforms/ConstraintElimination/decompose-signed-preconditions.ll
index a93f8615db127..49ca4d1cd7706 100644
--- a/llvm/test/Transforms/ConstraintElimination/decompose-signed-preconditions.ll
+++ b/llvm/test/Transforms/ConstraintElimination/decompose-signed-preconditions.ll
@@ -109,8 +109,7 @@ define i1 @sub_via_operand_signs(i8 %a, i8 %b) {
 ; CHECK-NEXT:    call void @llvm.assume(i1 [[C_1]])
 ; CHECK-NEXT:    [[C_2:%.*]] = icmp sge i8 [[A]], [[B]]
 ; CHECK-NEXT:    call void @llvm.assume(i1 [[C_2]])
-; CHECK-NEXT:    [[C:%.*]] = icmp sle i8 [[S]], [[A]]
-; CHECK-NEXT:    ret i1 [[C]]
+; CHECK-NEXT:    ret i1 true
 ;
 entry:
   %s = sub i8 %a, %b
@@ -143,3 +142,39 @@ entry:
   %c = icmp sle i8 %s, %a
   ret i1 %c
 }
+
+define i1 @sub_via_negative_constant_range(i8 %a) {
+; CHECK-LABEL: define i1 @sub_via_negative_constant_range(
+; CHECK-SAME: i8 [[A:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[S:%.*]] = sub i8 [[A]], -5
+; CHECK-NEXT:    call void @use(i8 [[S]])
+; CHECK-NEXT:    [[C_1:%.*]] = icmp sle i8 [[A]], 100
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C_1]])
+; CHECK-NEXT:    ret i1 true
+;
+entry:
+  %s = sub i8 %a, -5
+  call void @use(i8 %s)
+  %c.1 = icmp sle i8 %a, 100
+  call void @llvm.assume(i1 %c.1)
+  %c = icmp sge i8 %s, %a
+  ret i1 %c
+}
+
+; Without an upper bound on %a, the subtract may wrap.
+define i1 @sub_no_bound_for_negative_constant(i8 %a) {
+; CHECK-LABEL: define i1 @sub_no_bound_for_negative_constant(
+; CHECK-SAME: i8 [[A:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[S:%.*]] = sub i8 [[A]], -5
+; CHECK-NEXT:    call void @use(i8 [[S]])
+; CHECK-NEXT:    [[C:%.*]] = icmp sge i8 [[S]], [[A]]
+; CHECK-NEXT:    ret i1 [[C]]
+;
+entry:
+  %s = sub i8 %a, -5
+  call void @use(i8 %s)
+  %c = icmp sge i8 %s, %a
+  ret i1 %c
+}
diff --git a/llvm/test/Transforms/ConstraintElimination/materialize-flags.ll b/llvm/test/Transforms/ConstraintElimination/materialize-flags.ll
index ef858b2252639..b8cc032274b2d 100644
--- a/llvm/test/Transforms/ConstraintElimination/materialize-flags.ll
+++ b/llvm/test/Transforms/ConstraintElimination/materialize-flags.ll
@@ -147,6 +147,63 @@ exit:
   ret i32 0
 }
 
+; 0 <=s %b <=s %a, which implies nuw nsw.
+define i32 @sub_nsw_relational(i32 %a, i32 %b) {
+; CHECK-LABEL: define i32 @sub_nsw_relational(
+; CHECK-SAME: i32 [[A:%.*]], i32 [[B:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[C_0:%.*]] = icmp sge i32 [[B]], 0
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C_0]])
+; CHECK-NEXT:    [[C_1:%.*]] = icmp sge i32 [[A]], [[B]]
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C_1]])
+; CHECK-NEXT:    [[SUB:%.*]] = sub nuw nsw i32 [[A]], [[B]]
+; CHECK-NEXT:    ret i32 [[SUB]]
+;
+entry:
+  %c.0 = icmp sge i32 %b, 0
+  call void @llvm.assume(i1 %c.0)
+  %c.1 = icmp sge i32 %a, %b
+  call void @llvm.assume(i1 %c.1)
+  %sub = sub i32 %a, %b
+  ret i32 %sub
+}
+
+define i32 @sub_nuw_gains_nsw(i32 %a, i32 %b) {
+; CHECK-LABEL: define i32 @sub_nuw_gains_nsw(
+; CHECK-SAME: i32 [[A:%.*]], i32 [[B:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[C_0:%.*]] = icmp sge i32 [[B]], 0
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C_0]])
+; CHECK-NEXT:    [[C_1:%.*]] = icmp sge i32 [[A]], [[B]]
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C_1]])
+; CHECK-NEXT:    [[SUB:%.*]] = sub nuw nsw i32 [[A]], [[B]]
+; CHECK-NEXT:    ret i32 [[SUB]]
+;
+entry:
+  %c.0 = icmp sge i32 %b, 0
+  call void @llvm.assume(i1 %c.0)
+  %c.1 = icmp sge i32 %a, %b
+  call void @llvm.assume(i1 %c.1)
+  %sub = sub nuw i32 %a, %b
+  ret i32 %sub
+}
+
+define i32 @sub_nsw_constant_operand(i32 %a) {
+; CHECK-LABEL: define i32 @sub_nsw_constant_operand(
+; CHECK-SAME: i32 [[A:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[C:%.*]] = icmp sle i32 [[A]], 100
+; CHECK-NEXT:    call void @llvm.assume(i1 [[C]])
+; CHECK-NEXT:    [[SUB:%.*]] = sub nsw i32 [[A]], -5
+; CHECK-NEXT:    ret i32 [[SUB]]
+;
+entry:
+  %c = icmp sle i32 %a, 100
+  call void @llvm.assume(i1 %c)
+  %sub = sub i32 %a, -5
+  ret i32 %sub
+}
+
 define ptr @gep_nuw_transitive(ptr %p, i64 %i, i64 %j) {
 ; CHECK-LABEL: define ptr @gep_nuw_transitive(
 ; CHECK-SAME: ptr [[P:%.*]], i64 [[I:%.*]], i64 [[J:%.*]]) {
