diff --git a/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp b/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
index efe1920486df6..047a5f87c1955 100644
--- a/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
+++ b/llvm/lib/Transforms/Scalar/ConstraintElimination.cpp
@@ -552,6 +552,15 @@ static Decomposition decompose(Value *V, const ConstraintInfo &Info,
       return V;
     }
 
+    // `xor X, -1` is the bitwise complement of X, which as a signed value is
+    // exactly `-1 - X` for every X, with no wrapping and no precondition.
+    if (match(V, m_Not(m_Value(Op0)))) {
+      Decomposition Result(-1);
+      if (!Result.sub(decompose(Op0, Info, IsSigned, DL)))
+        return Result;
+      return V;
+    }
+
     if (match(V, m_NSWSub(m_Value(Op0), m_Value(Op1)))) {
       auto ResA = decompose(Op0, Info, IsSigned, DL);
       auto ResB = decompose(Op1, Info, IsSigned, DL);
diff --git a/llvm/test/Transforms/ConstraintElimination/xor-not-decompose.ll b/llvm/test/Transforms/ConstraintElimination/xor-not-decompose.ll
new file mode 100644
index 0000000000000..66b13c254b011
--- /dev/null
+++ b/llvm/test/Transforms/ConstraintElimination/xor-not-decompose.ll
@@ -0,0 +1,86 @@
+; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 5
+; RUN: opt -passes=constraint-elimination -S %s | FileCheck %s
+
+declare void @llvm.trap()
+
+define void @not_decomposed_signed(i64 %count, i64 %i) {
+; CHECK-LABEL: define void @not_decomposed_signed(
+; CHECK-SAME: i64 [[COUNT:%.*]], i64 [[I:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[NN:%.*]] = icmp sgt i64 [[COUNT]], -1
+; CHECK-NEXT:    br i1 [[NN]], label %[[C1:.*]], label %[[OUT:.*]]
+; CHECK:       [[C1]]:
+; CHECK-NEXT:    [[INN:%.*]] = icmp sgt i64 [[I]], -1
+; CHECK-NEXT:    br i1 [[INN]], label %[[C2:.*]], label %[[OUT]]
+; CHECK:       [[C2]]:
+; CHECK-NEXT:    [[LT:%.*]] = icmp slt i64 [[I]], [[COUNT]]
+; CHECK-NEXT:    br i1 [[LT]], label %[[C3:.*]], label %[[OUT]]
+; CHECK:       [[C3]]:
+; CHECK-NEXT:    [[NEG:%.*]] = xor i64 [[I]], -1
+; CHECK-NEXT:    [[IDX:%.*]] = add nsw i64 [[COUNT]], [[NEG]]
+; CHECK-NEXT:    br i1 true, label %[[OUT]], label %[[TRAP:.*]]
+; CHECK:       [[TRAP]]:
+; CHECK-NEXT:    call void @llvm.trap()
+; CHECK-NEXT:    unreachable
+; CHECK:       [[OUT]]:
+; CHECK-NEXT:    ret void
+;
+entry:
+  %nn = icmp sgt i64 %count, -1
+  br i1 %nn, label %c1, label %out
+
+c1:
+  %inn = icmp sgt i64 %i, -1
+  br i1 %inn, label %c2, label %out
+
+c2:
+  %lt = icmp slt i64 %i, %count
+  br i1 %lt, label %c3, label %out
+
+c3:
+  %neg = xor i64 %i, -1
+  %idx = add nsw i64 %count, %neg
+  %ok = icmp slt i64 %idx, %count
+  br i1 %ok, label %out, label %trap
+
+trap:
+  call void @llvm.trap()
+  unreachable
+
+out:
+  ret void
+}
+
+; Negative test: the decomposition is only valid in the signed system.
+define void @not_decomposed_unsigned(i64 %x) {
+; CHECK-LABEL: define void @not_decomposed_unsigned(
+; CHECK-SAME: i64 [[X:%.*]]) {
+; CHECK-NEXT:  [[ENTRY:.*:]]
+; CHECK-NEXT:    [[Z:%.*]] = icmp eq i64 [[X]], 0
+; CHECK-NEXT:    br i1 [[Z]], label %[[C:.*]], label %[[OUT:.*]]
+; CHECK:       [[C]]:
+; CHECK-NEXT:    [[N:%.*]] = xor i64 [[X]], -1
+; CHECK-NEXT:    [[OK:%.*]] = icmp ult i64 [[N]], 5
+; CHECK-NEXT:    br i1 [[OK]], label %[[BAD:.*]], label %[[OUT]]
+; CHECK:       [[BAD]]:
+; CHECK-NEXT:    call void @llvm.trap()
+; CHECK-NEXT:    unreachable
+; CHECK:       [[OUT]]:
+; CHECK-NEXT:    ret void
+;
+entry:
+  %z = icmp eq i64 %x, 0
+  br i1 %z, label %c, label %out
+
+c:
+  %n = xor i64 %x, -1
+  %ok = icmp ult i64 %n, 5
+  br i1 %ok, label %bad, label %out
+
+bad:
+  call void @llvm.trap()
+  unreachable
+
+out:
+  ret void
+}
