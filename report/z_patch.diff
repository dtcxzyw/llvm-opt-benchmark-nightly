diff --git a/llvm/lib/Transforms/InstCombine/InstCombineMulDivRem.cpp b/llvm/lib/Transforms/InstCombine/InstCombineMulDivRem.cpp
index 128e3e3dcdfe8..390df94b6c2ac 100644
--- a/llvm/lib/Transforms/InstCombine/InstCombineMulDivRem.cpp
+++ b/llvm/lib/Transforms/InstCombine/InstCombineMulDivRem.cpp
@@ -497,12 +497,10 @@ Instruction *InstCombinerImpl::visitMul(BinaryOperator &I) {
 
   // Fold the following two scenarios:
   //   1) i1 mul -> i1 and.
-  //   2) X * Y --> X & Y, iff X, Y can be only {0,1}.
-  // Note: We could use known bits to generalize this and related patterns with
-  // shifts/truncs
+  //   2) X * Y --> X & Y, iff X, Y are loads known to be in the range {0,1}.
   if (Ty->isIntOrIntVectorTy(1) ||
-      (match(Op0, m_And(m_Value(), m_One())) &&
-       match(Op1, m_And(m_Value(), m_One()))))
+      ((match(Op0, m_And(m_Value(), m_One())) || (isa<LoadInst>(Op0) && computeKnownBits(Op0, &I).countMaxActiveBits() <= 1)) &&
+       (match(Op1, m_And(m_Value(), m_One())) || (isa<LoadInst>(Op1) && computeKnownBits(Op1, &I).countMaxActiveBits() <= 1))))
     return BinaryOperator::CreateAnd(Op0, Op1);
 
   if (Value *R = foldMulShl1(I, /* CommuteOperands */ false, Builder))
