diff --git a/llvm/lib/Analysis/ScalarEvolution.cpp b/llvm/lib/Analysis/ScalarEvolution.cpp
index 27a1a20bcdf79..eaf9f27de2f2f 100644
--- a/llvm/lib/Analysis/ScalarEvolution.cpp
+++ b/llvm/lib/Analysis/ScalarEvolution.cpp
@@ -12943,6 +12943,41 @@ static bool IsKnownPredicateViaAddRecStart(ScalarEvolution &SE,
   return SE.isKnownPredicate(Pred, LStart, RStart);
 }
 
+/// Is LHS `Pred` RHS true because one of them is an AddRec that is known not to
+/// go below its own start value, which is the other side?
+static bool IsKnownPredicateViaAddRecMonotonicity(ScalarEvolution &SE,
+                                                  CmpPredicate Pred,
+                                                  const SCEV *LHS,
+                                                  const SCEV *RHS) {
+  if (!ICmpInst::isRelational(Pred))
+    return false;
+
+  // Normalize to (AddRec Pred Start).
+  if (!isa<SCEVAddRecExpr>(LHS) && isa<SCEVAddRecExpr>(RHS)) {
+    Pred = ICmpInst::getSwappedCmpPredicate(Pred);
+    std::swap(LHS, RHS);
+  }
+
+  const auto *AR = dyn_cast<SCEVAddRecExpr>(LHS);
+  if (!AR || AR->getStart() != RHS)
+    return false;
+
+  // With FlagNUW every step adds without unsigned wrap, so the recurrence
+  // cannot become unsigned-smaller than the value it started at, whatever the
+  // steps are. In the signed sense the same holds with FlagNSW, as long as the
+  // step cannot be negative.
+  bool IsSigned = ICmpInst::isSigned(Pred);
+  if (!AR->getNoWrapFlags(IsSigned ? SCEV::FlagNSW : SCEV::FlagNUW))
+    return false;
+  if (IsSigned && (!AR->isAffine() ||
+                   !SE.isKnownNonNegative(AR->getStepRecurrence(SE))))
+    return false;
+
+  // The recurrence is equal to Start in the first iteration, so only the
+  // non-strict predicate holds.
+  return Pred == (IsSigned ? ICmpInst::ICMP_SGE : ICmpInst::ICMP_UGE);
+}
+
 /// Is LHS `Pred` RHS true on the virtue of LHS or RHS being a Min or Max
 /// expression?
 static bool IsKnownPredicateViaMinOrMax(ScalarEvolution &SE, CmpPredicate Pred,
@@ -13203,6 +13238,7 @@ bool ScalarEvolution::isKnownViaNonRecursiveReasoning(CmpPredicate Pred,
          isKnownPredicateViaConstantRanges(Pred, LHS, RHS) ||
          IsKnownPredicateViaMinOrMax(*this, Pred, LHS, RHS) ||
          IsKnownPredicateViaAddRecStart(*this, Pred, LHS, RHS) ||
+         IsKnownPredicateViaAddRecMonotonicity(*this, Pred, LHS, RHS) ||
          isKnownPredicateViaNoOverflow(Pred, LHS, RHS);
 }
 
diff --git a/llvm/test/Analysis/ScalarEvolution/addrec-vs-start-monotonic.ll b/llvm/test/Analysis/ScalarEvolution/addrec-vs-start-monotonic.ll
new file mode 100644
index 0000000000000..2c72917e65283
--- /dev/null
+++ b/llvm/test/Analysis/ScalarEvolution/addrec-vs-start-monotonic.ll
@@ -0,0 +1,328 @@
+; NOTE: Assertions have been autogenerated by utils/update_analyze_test_checks.py
+; RUN: opt -disable-output -passes='loop-simplify,print<scalar-evolution>' %s 2>&1 | FileCheck %s
+
+; The %up loop has no computable exit count, so its recurrence stays an operand
+; of the exit count of %down rather than being evaluated to a closed form. The
+; recurrence has nuw, so it cannot be unsigned-below the %start it began at, and
+; the umin against %start folds away.
+define void @dec_to_start_of_nuw_ptr_addrec(ptr %start, i32 %n) {
+; CHECK-LABEL: 'dec_to_start_of_nuw_ptr_addrec'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_nuw_ptr_addrec
+; CHECK-NEXT:    %p = phi ptr [ %start, %entry ], [ %p.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,1}<nuw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %p.next = getelementptr nuw i8, ptr %p, i64 1
+; CHECK-NEXT:    --> {(1 + %start),+,1}<nw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %q = phi ptr [ %q.next, %down.latch ], [ %p, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,1}<nuw><%up>,+,-1}<%down> U: full-set S: full-set Exits: %start LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %q.next = getelementptr i8, ptr %q, i64 -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,1}<nw><%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_nuw_ptr_addrec
+; CHECK-NEXT:  Loop %down: backedge-taken count is {0,+,1}<nuw><%up>
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is {0,+,1}<nuw><%up>
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  br label %up
+
+up:
+  %p = phi ptr [ %start, %entry ], [ %p.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %p.next = getelementptr nuw i8, ptr %p, i64 1
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %q = phi ptr [ %p, %up ], [ %q.next, %down.latch ]
+  %in.range = icmp ugt ptr %q, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %q.next = getelementptr i8, ptr %q, i64 -1
+  br label %down
+
+exit:
+  ret void
+}
+
+; Same for an integer recurrence.
+define void @dec_to_start_of_nuw_int_addrec(i64 %start, i32 %n) {
+; CHECK-LABEL: 'dec_to_start_of_nuw_int_addrec'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_nuw_int_addrec
+; CHECK-NEXT:    %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,1}<nuw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %x.next = add nuw i64 %x, 1
+; CHECK-NEXT:    --> {(1 + %start),+,1}<nw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %y = phi i64 [ %y.next, %down.latch ], [ %x, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,1}<nuw><%up>,+,-1}<%down> U: full-set S: full-set Exits: %start LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %y.next = add i64 %y, -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,1}<nw><%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_nuw_int_addrec
+; CHECK-NEXT:  Loop %down: backedge-taken count is {0,+,1}<nuw><%up>
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is {0,+,1}<nuw><%up>
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  br label %up
+
+up:
+  %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %x.next = add nuw i64 %x, 1
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %y = phi i64 [ %x, %up ], [ %y.next, %down.latch ]
+  %in.range = icmp ugt i64 %y, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %y.next = add i64 %y, -1
+  br label %down
+
+exit:
+  ret void
+}
+
+; Negative test: the recurrence may wrap, so it can be below %start and the
+; clamp has to stay.
+define void @dec_to_start_of_wrapping_ptr_addrec(ptr %start, i32 %n) {
+; CHECK-LABEL: 'dec_to_start_of_wrapping_ptr_addrec'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_wrapping_ptr_addrec
+; CHECK-NEXT:    %p = phi ptr [ %start, %entry ], [ %p.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,1}<%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %p.next = getelementptr i8, ptr %p, i64 1
+; CHECK-NEXT:    --> {(1 + %start),+,1}<%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %q = phi ptr [ %q.next, %down.latch ], [ %p, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,1}<%up>,+,-1}<%down> U: full-set S: full-set Exits: ((-1 * (ptrtoaddr ptr %start to i64)) + ({(ptrtoaddr ptr %start to i64),+,1}<%up> umin (ptrtoaddr ptr %start to i64)) + %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %q.next = getelementptr i8, ptr %q, i64 -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,1}<%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + (-1 * (ptrtoaddr ptr %start to i64)) + ({(ptrtoaddr ptr %start to i64),+,1}<%up> umin (ptrtoaddr ptr %start to i64)) + %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_wrapping_ptr_addrec
+; CHECK-NEXT:  Loop %down: backedge-taken count is ((-1 * ({(ptrtoaddr ptr %start to i64),+,1}<%up> umin (ptrtoaddr ptr %start to i64))) + {(ptrtoaddr ptr %start to i64),+,1}<%up>)
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is ((-1 * ({(ptrtoaddr ptr %start to i64),+,1}<%up> umin (ptrtoaddr ptr %start to i64))) + {(ptrtoaddr ptr %start to i64),+,1}<%up>)
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  br label %up
+
+up:
+  %p = phi ptr [ %start, %entry ], [ %p.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %p.next = getelementptr i8, ptr %p, i64 1
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %q = phi ptr [ %p, %up ], [ %q.next, %down.latch ]
+  %in.range = icmp ugt ptr %q, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %q.next = getelementptr i8, ptr %q, i64 -1
+  br label %down
+
+exit:
+  ret void
+}
+
+; The signed rule: with nsw and a step that cannot be negative, the recurrence
+; cannot be signed-below the %start it began at, so the smin folds away.
+define void @dec_to_start_of_nsw_addrec(i64 %start, i32 %n, i64 %step.raw) {
+; CHECK-LABEL: 'dec_to_start_of_nsw_addrec'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_nsw_addrec
+; CHECK-NEXT:    %step = and i64 %step.raw, 7
+; CHECK-NEXT:    --> (zext i3 (trunc i64 %step.raw to i3) to i64) U: [0,8) S: [0,8)
+; CHECK-NEXT:    %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %x.next = add nsw i64 %x, %step
+; CHECK-NEXT:    --> {((zext i3 (trunc i64 %step.raw to i3) to i64) + %start),+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %y = phi i64 [ %y.next, %down.latch ], [ %x, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up>,+,-1}<%down> U: full-set S: full-set Exits: %start LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %y.next = add i64 %y, -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nw><%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_nsw_addrec
+; CHECK-NEXT:  Loop %down: backedge-taken count is {0,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nuw><%up>
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is {0,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nuw><%up>
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  %step = and i64 %step.raw, 7
+  br label %up
+
+up:
+  %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %x.next = add nsw i64 %x, %step
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %y = phi i64 [ %x, %up ], [ %y.next, %down.latch ]
+  %in.range = icmp sgt i64 %y, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %y.next = add i64 %y, -1
+  br label %down
+
+exit:
+  ret void
+}
+
+; Negative test: nsw alone is not enough in the signed sense, the step also has
+; to be known non-negative. Here it is not, so the smin has to stay.
+define void @dec_to_start_of_nsw_addrec_unknown_step(i64 %start, i32 %n, i64 %step) {
+; CHECK-LABEL: 'dec_to_start_of_nsw_addrec_unknown_step'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_nsw_addrec_unknown_step
+; CHECK-NEXT:    %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,%step}<nsw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %x.next = add nsw i64 %x, %step
+; CHECK-NEXT:    --> {(%start + %step),+,%step}<nw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %y = phi i64 [ %y.next, %down.latch ], [ %x, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,%step}<nsw><%up>,+,-1}<%down> U: full-set S: full-set Exits: ({%start,+,%step}<nsw><%up> smin %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %y.next = add i64 %y, -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,%step}<nw><%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + ({%start,+,%step}<nsw><%up> smin %start)) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_nsw_addrec_unknown_step
+; CHECK-NEXT:  Loop %down: backedge-taken count is ((-1 * ({%start,+,%step}<nsw><%up> smin %start)) + {%start,+,%step}<nsw><%up>)
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is ((-1 * ({%start,+,%step}<nsw><%up> smin %start)) + {%start,+,%step}<nsw><%up>)
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  br label %up
+
+up:
+  %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %x.next = add nsw i64 %x, %step
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %y = phi i64 [ %x, %up ], [ %y.next, %down.latch ]
+  %in.range = icmp sgt i64 %y, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %y.next = add i64 %y, -1
+  br label %down
+
+exit:
+  ret void
+}
+
+; Negative test: nsw gives the signed rule only. The recurrence below is nsw with
+; a non-negative step but the clamp is unsigned, and a nsw recurrence can wrap
+; around the unsigned boundary, so the umin has to stay.
+define void @dec_to_start_of_nsw_addrec_unsigned_clamp(i64 %start, i32 %n, i64 %step.raw) {
+; CHECK-LABEL: 'dec_to_start_of_nsw_addrec_unsigned_clamp'
+; CHECK-NEXT:  Classifying expressions for: @dec_to_start_of_nsw_addrec_unsigned_clamp
+; CHECK-NEXT:    %step = and i64 %step.raw, 7
+; CHECK-NEXT:    --> (zext i3 (trunc i64 %step.raw to i3) to i64) U: [0,8) S: [0,8)
+; CHECK-NEXT:    %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+; CHECK-NEXT:    --> {%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+; CHECK-NEXT:    --> %v U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %x.next = add nsw i64 %x, %step
+; CHECK-NEXT:    --> {((zext i3 (trunc i64 %step.raw to i3) to i64) + %start),+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nw><%up> U: full-set S: full-set Exits: <<Unknown>> LoopDispositions: { %up: Computable }
+; CHECK-NEXT:    %v.next = mul i32 %v, 10
+; CHECK-NEXT:    --> (10 * %v) U: [0,-1) S: [-2147483648,2147483647) Exits: <<Unknown>> LoopDispositions: { %up: Variant }
+; CHECK-NEXT:    %y = phi i64 [ %y.next, %down.latch ], [ %x, %down.preheader ]
+; CHECK-NEXT:    --> {{\{\{}}%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up>,+,-1}<%down> U: full-set S: full-set Exits: ({%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> umin %start) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:    %y.next = add i64 %y, -1
+; CHECK-NEXT:    --> {{\{\{}}(-1 + %start),+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nw><%up>,+,-1}<%down> U: full-set S: full-set Exits: (-1 + ({%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> umin %start)) LoopDispositions: { %down: Computable }
+; CHECK-NEXT:  Determining loop execution counts for: @dec_to_start_of_nsw_addrec_unsigned_clamp
+; CHECK-NEXT:  Loop %down: backedge-taken count is ((-1 * ({%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> umin %start)) + {%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up>)
+; CHECK-NEXT:  Loop %down: constant max backedge-taken count is i64 -1
+; CHECK-NEXT:  Loop %down: symbolic max backedge-taken count is ((-1 * ({%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up> umin %start)) + {%start,+,(zext i3 (trunc i64 %step.raw to i3) to i64)}<nsw><%up>)
+; CHECK-NEXT:  Loop %down: Trip multiple is 1
+; CHECK-NEXT:  Loop %up: Unpredictable backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable constant max backedge-taken count.
+; CHECK-NEXT:  Loop %up: Unpredictable symbolic max backedge-taken count.
+;
+entry:
+  %step = and i64 %step.raw, 7
+  br label %up
+
+up:
+  %x = phi i64 [ %start, %entry ], [ %x.next, %up.latch ]
+  %v = phi i32 [ %n, %entry ], [ %v.next, %up.latch ]
+  %pos = icmp sgt i32 %v, 0
+  br i1 %pos, label %up.latch, label %down
+
+up.latch:
+  %x.next = add nsw i64 %x, %step
+  %v.next = mul i32 %v, 10
+  br label %up
+
+down:
+  %y = phi i64 [ %x, %up ], [ %y.next, %down.latch ]
+  %in.range = icmp ugt i64 %y, %start
+  br i1 %in.range, label %down.latch, label %exit
+
+down.latch:
+  %y.next = add i64 %y, -1
+  br label %down
+
+exit:
+  ret void
+}
