diff --git a/llvm/lib/Analysis/ValueTracking.cpp b/llvm/lib/Analysis/ValueTracking.cpp
index 3be0db5ad9c7e..e2bec5ba4286f 100644
--- a/llvm/lib/Analysis/ValueTracking.cpp
+++ b/llvm/lib/Analysis/ValueTracking.cpp
@@ -9690,10 +9690,21 @@ isImpliedCondICmps(CmpPredicate LPred, const Value *L0, const Value *L1,
       return true;
   }
 
-  // a - b == NonZero -> a != b
-  // ptrtoint(a) - ptrtoint(b) == NonZero -> a != b
   const APInt *L1C;
   Value *A, *B;
+  // A != B implies (A | B) != 0 and disproves (A | B) == 0.
+  if (LPred == ICmpInst::ICMP_NE && ICmpInst::isEquality(RPred) &&
+      match(R1, m_Zero()) && match(R0, m_c_Or(m_Specific(L0), m_Specific(L1))))
+    return RPred == ICmpInst::ICMP_NE;
+
+  // (A | B) == 0 implies A == B and disproves A != B.
+  if (LPred == ICmpInst::ICMP_EQ && ICmpInst::isEquality(RPred) &&
+      match(L1, m_Zero()) && match(L0, m_c_Or(m_Value(A), m_Value(B))) &&
+      ((R0 == A && R1 == B) || (R0 == B && R1 == A)))
+    return RPred == ICmpInst::ICMP_EQ;
+
+  // a - b == NonZero -> a != b
+  // ptrtoint(a) - ptrtoint(b) == NonZero -> a != b
   if (LPred == ICmpInst::ICMP_EQ && ICmpInst::isEquality(RPred) &&
       match(L1, m_APInt(L1C)) && !L1C->isZero() &&
       match(L0, m_Sub(m_Value(A), m_Value(B))) &&
diff --git a/llvm/test/Transforms/InstSimplify/and-or-icmp-or-zero.ll b/llvm/test/Transforms/InstSimplify/and-or-icmp-or-zero.ll
new file mode 100644
index 0000000000000..f7d1b7eb9a2ce
--- /dev/null
+++ b/llvm/test/Transforms/InstSimplify/and-or-icmp-or-zero.ll
@@ -0,0 +1,80 @@
+; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
+; RUN: opt -passes=instsimplify < %s -S | FileCheck %s
+
+define i1 @and_ne_or_ne(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @and_ne_or_ne(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    [[CMP:%.*]] = icmp ne i64 [[A]], [[B]]
+; CHECK-NEXT:    ret i1 [[CMP]]
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp ne i64 %a, %b
+  %cmp1 = icmp ne i64 %or, 0
+  %r = and i1 %cmp0, %cmp1
+  ret i1 %r
+}
+
+define i1 @or_eq_or_eq_zero(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @or_eq_or_eq_zero(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    [[CMP:%.*]] = icmp eq i64 [[A]], [[B]]
+; CHECK-NEXT:    ret i1 [[CMP]]
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp eq i64 %a, %b
+  %cmp1 = icmp eq i64 %or, 0
+  %r = or i1 %cmp0, %cmp1
+  ret i1 %r
+}
+
+define i1 @and_eq_or_eq_zero(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @and_eq_or_eq_zero(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    [[OR:%.*]] = or i64 [[A]], [[B]]
+; CHECK-NEXT:    [[CMP:%.*]] = icmp eq i64 [[OR]], 0
+; CHECK-NEXT:    ret i1 [[CMP]]
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp eq i64 %a, %b
+  %cmp1 = icmp eq i64 %or, 0
+  %r = and i1 %cmp0, %cmp1
+  ret i1 %r
+}
+
+define i1 @or_ne_or_ne_zero(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @or_ne_or_ne_zero(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    [[OR:%.*]] = or i64 [[A]], [[B]]
+; CHECK-NEXT:    [[CMP:%.*]] = icmp ne i64 [[OR]], 0
+; CHECK-NEXT:    ret i1 [[CMP]]
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp ne i64 %a, %b
+  %cmp1 = icmp ne i64 %or, 0
+  %r = or i1 %cmp0, %cmp1
+  ret i1 %r
+}
+
+define i1 @and_ne_or_eq_zero(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @and_ne_or_eq_zero(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    ret i1 false
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp ne i64 %a, %b
+  %cmp1 = icmp eq i64 %or, 0
+  %r = and i1 %cmp0, %cmp1
+  ret i1 %r
+}
+
+define i1 @or_eq_or_ne_zero(i64 %a, i64 %b) {
+; CHECK-LABEL: define i1 @or_eq_or_ne_zero(
+; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
+; CHECK-NEXT:    ret i1 true
+;
+  %or = or i64 %a, %b
+  %cmp0 = icmp eq i64 %a, %b
+  %cmp1 = icmp ne i64 %or, 0
+  %r = or i1 %cmp0, %cmp1
+  ret i1 %r
+}
