diff --git a/llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp b/llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp
index 58605a5da3de8..2878b9fab8350 100644
--- a/llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp
+++ b/llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp
@@ -657,41 +657,6 @@ Instruction *InstCombinerImpl::foldSelectIntoOp(SelectInst &SI, Value *TrueVal,
   return nullptr;
 }
 
-static Value *canoncalizeSelectICmpMinMax(const ICmpInst *Cmp, Value *TVal,
-                                          Value *FVal,
-                                          InstCombiner::BuilderTy &Builder,
-                                          const SimplifyQuery &SQ) {
-  Value *CmpLHS = Cmp->getOperand(0);
-  Value *CmpRHS = Cmp->getOperand(1);
-  ICmpInst::Predicate Pred = Cmp->getPredicate();
-  if (match(FVal, m_Zero())) {
-    std::swap(TVal, FVal);
-    Pred = ICmpInst::getInversePredicate(Pred);
-  }
-  if (!match(TVal, m_Zero()))
-    return nullptr;
-
-  if (Pred == CmpInst::ICMP_SGT || Pred == CmpInst::ICMP_SGE) {
-    std::swap(CmpLHS, CmpRHS);
-    Pred = ICmpInst::getSwappedPredicate(Pred);
-  }
-
-  // Handles:
-  // (X <= Y) ? 0 : (X - Y)
-  // (X <= Y) ? (Y - X) : 0
-  // (X >= Y) ? 0 : (Y - X)
-  // (X >= Y) ? (X - Y) : 0
-  if ((Pred == CmpInst::ICMP_SLT || Pred == CmpInst::ICMP_SLE) &&
-      match(FVal, m_NSWSub(m_Specific(CmpLHS), m_Specific(CmpRHS))) &&
-      isGuaranteedNotToBeUndef(CmpLHS, SQ.AC, SQ.CxtI, SQ.DT)) {
-    Value *SMin =
-        Builder.CreateBinaryIntrinsic(Intrinsic::smin, CmpRHS, CmpLHS);
-    return Builder.CreateNSWSub(CmpLHS, SMin);
-  }
-
-  return nullptr;
-}
-
 /// Try to fold a select to a min/max intrinsic. Many cases are already handled
 /// by matchDecomposedSelectPattern but here we handle the cases where more
 /// extensive modification of the IR is required.
@@ -699,13 +664,10 @@ static Value *foldSelectICmpMinMax(const ICmpInst *Cmp, Value *TVal,
                                    Value *FVal,
                                    InstCombiner::BuilderTy &Builder,
                                    const SimplifyQuery &SQ) {
-  Value *CmpLHS = Cmp->getOperand(0);
-  Value *CmpRHS = Cmp->getOperand(1);
+  const Value *CmpLHS = Cmp->getOperand(0);
+  const Value *CmpRHS = Cmp->getOperand(1);
   ICmpInst::Predicate Pred = Cmp->getPredicate();
 
-  if (Value *V = canoncalizeSelectICmpMinMax(Cmp, TVal, FVal, Builder, SQ))
-    return V;
-
   // (X > Y) ? X : (Y - 1) ==> MIN(X, Y - 1)
   // (X < Y) ? X : (Y + 1) ==> MAX(X, Y + 1)
   // This transformation is valid when overflow corresponding to the sign of
@@ -3691,7 +3653,7 @@ static Instruction *foldNestedSelects(SelectInst &OuterSelVal,
 /// already poison. For example, if ValAssumedPoison is `icmp samesign X, 10`
 /// and V is `icmp ne X, 5`, impliesPoisonOrCond returns true.
 static bool impliesPoisonOrCond(const Value *ValAssumedPoison, const Value *V,
-                                bool Expected, const SimplifyQuery &SQ) {
+                                bool Expected) {
   if (impliesPoison(ValAssumedPoison, V))
     return true;
 
@@ -3716,14 +3678,6 @@ static bool impliesPoisonOrCond(const Value *ValAssumedPoison, const Value *V,
                       *RHSC2);
     }
   }
-  Value *A;
-  if (match(ValAssumedPoison, m_NUWTrunc(m_Value(A))) &&
-      isGuaranteedNotToBePoison(A)) {
-    assert(ValAssumedPoison->getType()->isIntOrIntVectorTy(1));
-    return computeKnownBits(
-               A, SQ.getWithInstruction(cast<Instruction>(ValAssumedPoison)))
-               .getMaxValue() == 1;
-  }
 
   return false;
 }
@@ -3749,13 +3703,13 @@ Instruction *InstCombinerImpl::foldSelectOfBools(SelectInst &SI) {
   // checks whether folding it does not convert a well-defined value into
   // poison.
   if (match(TrueVal, m_One())) {
-    if (impliesPoisonOrCond(FalseVal, CondVal, /*Expected=*/false, SQ)) {
+    if (impliesPoisonOrCond(FalseVal, CondVal, /*Expected=*/false)) {
       // Change: A = select B, true, C --> A = or B, C
       return BinaryOperator::CreateOr(CondVal, FalseVal);
     }
 
     if (match(CondVal, m_OneUse(m_Select(m_Value(A), m_One(), m_Value(B)))) &&
-        impliesPoisonOrCond(FalseVal, B, /*Expected=*/false, SQ)) {
+        impliesPoisonOrCond(FalseVal, B, /*Expected=*/false)) {
       // (A || B) || C --> A || (B | C)
       Value *LOr = Builder.CreateLogicalOr(A, Builder.CreateOr(B, FalseVal));
       if (auto *I = dyn_cast<Instruction>(LOr)) {
@@ -3795,13 +3749,13 @@ Instruction *InstCombinerImpl::foldSelectOfBools(SelectInst &SI) {
   }
 
   if (match(FalseVal, m_Zero())) {
-    if (impliesPoisonOrCond(TrueVal, CondVal, /*Expected=*/true, SQ)) {
+    if (impliesPoisonOrCond(TrueVal, CondVal, /*Expected=*/true)) {
       // Change: A = select B, C, false --> A = and B, C
       return BinaryOperator::CreateAnd(CondVal, TrueVal);
     }
 
     if (match(CondVal, m_OneUse(m_Select(m_Value(A), m_Value(B), m_Zero()))) &&
-        impliesPoisonOrCond(TrueVal, B, /*Expected=*/true, SQ)) {
+        impliesPoisonOrCond(TrueVal, B, /*Expected=*/true)) {
       // (A && B) && C --> A && (B & C)
       Value *LAnd = Builder.CreateLogicalAnd(A, Builder.CreateAnd(B, TrueVal));
       if (auto *I = dyn_cast<Instruction>(LAnd)) {
@@ -4463,48 +4417,125 @@ static Value *foldSelectBitTest(SelectInst &Sel, Value *CondVal, Value *TrueVal,
   return nullptr;
 }
 
-/// This function makes the following folds:
-/// select C, (sub 0, X), (xor X, -1)
-///   -> sub (sext !C), X
-/// select C, (xor X, -1), (sub 0, X)
-///   -> sub (sext C), X
-static Instruction *foldSelectNegNot(SelectInst &SI,
-                                     InstCombiner::BuilderTy &Builder) {
-  auto *CondVal = SI.getCondition();
-  auto *TrueVal = SI.getTrueValue();
-  auto *FalseVal = SI.getFalseValue();
-  auto *SelTy = SI.getType();
 
-  if (!SelTy->isIntOrIntVectorTy() || SelTy->isIntOrIntVectorTy(1))
+static bool isKnownULESignBit(Value *V, const SimplifyQuery &SQ,
+                              const Instruction *CtxI) {
+  auto *Ty = dyn_cast<IntegerType>(V->getType());
+  if (!Ty)
+    return false;
+
+  if (auto *C = dyn_cast<ConstantInt>(V)) {
+    const APInt &Val = C->getValue();
+    return Val.ule(APInt::getSignMask(Val.getBitWidth()));
+  }
+
+  // Sufficient condition for non-constant values:
+  // if the sign bit is known zero, then V < SignBit <= SignBit.
+  KnownBits Known = computeKnownBits(V, SQ.getWithInstruction(CtxI));
+  return Known.isNonNegative();
+}
+
+// Match:
+//   U = umax(X, C)
+//   Sum = add nsw U, X
+//   Ov = icmp ult Sum, X
+//   Clamp = umin(Sum, K)
+//   R = select Ov, K, Clamp
+//
+// If C <= SignBit, then Ov implies X >= SignBit. Otherwise, if X < SignBit,
+// U <= SignBit and X + U cannot unsigned-overflow.
+//
+// When Ov is true, U == X. Since Sum = add nsw X, X is defined and Ov is true,
+// Sum must also be in the unsigned range [SignBit, UINT_MAX].
+//
+// If K <= SignBit, then K <=u Sum on the true arm, so:
+//   umin(Sum, K) == K
+//
+// Therefore both select arms produce Clamp.
+static Value *foldSelectUAddNSWOverflowToUMinClamp(SelectInst &SI,
+                                                   const SimplifyQuery &SQ) {
+  if (!SI.getCondition()->getType()->isIntegerTy(1) ||
+      !SI.getType()->isIntegerTy())
     return nullptr;
 
-  if (CondVal->getType()->isVectorTy() != SelTy->isVectorTy())
+  auto *Cmp = dyn_cast<ICmpInst>(SI.getCondition());
+  if (!Cmp)
     return nullptr;
 
-  auto matchNegNot = [&](Value *Neg, Value *Not, Value *&X) -> bool {
-    return match(Neg, m_OneUse(m_Neg(m_Value(X)))) &&
-           match(Not, m_OneUse(m_Not(m_Specific(X))));
-  };
+  // Match:
+  //   %ov = icmp ult %sum, %x
+  //
+  // Also allow the equivalent:
+  //   %ov = icmp ugt %x, %sum
+  Value *Sum, *X;
+  if (Cmp->getPredicate() == ICmpInst::ICMP_ULT) {
+    Sum = Cmp->getOperand(0);
+    X = Cmp->getOperand(1);
+  } else if (Cmp->getPredicate() == ICmpInst::ICMP_UGT) {
+    X = Cmp->getOperand(0);
+    Sum = Cmp->getOperand(1);
+  } else {
+    return nullptr;
+  }
 
-  Value *X;
-  Value *Mask;
+  Value *K = SI.getTrueValue();
+  auto *Clamp = dyn_cast<MinMaxIntrinsic>(SI.getFalseValue());
+  if (!Clamp || Clamp->getIntrinsicID() != Intrinsic::umin)
+    return nullptr;
 
-  // select C, (sub 0, X), (xor X, -1) -> sub (sext !C), X
-  if (matchNegNot(TrueVal, FalseVal, X)) {
-    Value *NotCond = Builder.CreateNot(CondVal, "not." + CondVal->getName());
-    Mask = Builder.CreateSExt(NotCond, SelTy);
-    return BinaryOperator::CreateSub(Mask, X);
-  }
+  // Match:
+  //   %clamp = umin %sum, %K
+  //
+  // Allow commuted operands.
+  if (!((Clamp->getArgOperand(0) == Sum && Clamp->getArgOperand(1) == K) ||
+        (Clamp->getArgOperand(0) == K && Clamp->getArgOperand(1) == Sum)))
+    return nullptr;
 
-  // select C, (xor X, -1), (sub 0, X) -> sub (sext C), X
-  if (matchNegNot(FalseVal, TrueVal, X)) {
-    Mask = Builder.CreateSExt(CondVal, SelTy);
-    return BinaryOperator::CreateSub(Mask, X);
-  }
+  // Match:
+  //   %sum = add nsw %u, %x
+  //
+  // Allow commuted add operands.
+  auto *Add = dyn_cast<BinaryOperator>(Sum);
+  if (!Add || Add->getOpcode() != Instruction::Add || !Add->hasNoSignedWrap())
+    return nullptr;
 
-  return nullptr;
+  Value *U;
+  if (Add->getOperand(0) == X)
+    U = Add->getOperand(1);
+  else if (Add->getOperand(1) == X)
+    U = Add->getOperand(0);
+  else
+    return nullptr;
+
+  // Match:
+  //   %u = umax %x, %C
+  //
+  // Allow commuted operands.
+  auto *Umax = dyn_cast<MinMaxIntrinsic>(U);
+  if (!Umax || Umax->getIntrinsicID() != Intrinsic::umax)
+    return nullptr;
+
+  Value *C;
+  if (Umax->getArgOperand(0) == X)
+    C = Umax->getArgOperand(1);
+  else if (Umax->getArgOperand(1) == X)
+    C = Umax->getArgOperand(0);
+  else
+    return nullptr;
+
+  // Required:
+  //   C <=u SignBit
+  //   K <=u SignBit
+  //
+  // For non-constant values we currently use the stronger sufficient condition
+  // that the sign bit is known zero.
+  if (!isKnownULESignBit(C, SQ, &SI) || !isKnownULESignBit(K, SQ, &SI))
+    return nullptr;
+
+  return Clamp;
 }
 
+
 Instruction *InstCombinerImpl::visitSelectInst(SelectInst &SI) {
   Value *CondVal = SI.getCondition();
   Value *TrueVal = SI.getTrueValue();
@@ -4593,9 +4624,6 @@ Instruction *InstCombinerImpl::visitSelectInst(SelectInst &SI) {
     }
   }
 
-  if (Instruction *I = foldSelectNegNot(SI, Builder))
-    return I;
-
   auto *SIFPOp = dyn_cast<FPMathOperator>(&SI);
 
   if (auto *FCmp = dyn_cast<FCmpInst>(CondVal)) {
@@ -5319,5 +5347,8 @@ Instruction *InstCombinerImpl::visitSelectInst(SelectInst &SI) {
     return CallInst::Create(Scmp, {CmpLHS, ConstantInt::get(SI.getType(), 0)});
   }
 
+  if (Value *V = foldSelectUAddNSWOverflowToUMinClamp(SI, SQ))
+  return replaceInstUsesWith(SI, V);
+
   return nullptr;
 }
